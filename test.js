const { PrismaClient } = require("@prisma/client"); const bcrypt = require("bcrypt"); const prisma = new PrismaClient(); async function main() { const hash = await bcrypt.hash("password123", 10); const user = await prisma.user.create({ data: { email: "team2@test.com", passwordHash: hash, role: "COMPANY" } }); const company = await prisma.companyProfile.findFirst(); await prisma.companyMember.create({ data: { userId: user.id, companyId: company.id } }); console.log("Created"); } main();
